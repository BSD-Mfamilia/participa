<p>¡<%= t('podemos.devise.mailer.welcome') %> <%= @resource.first_name %> <%= @resource.last_name %>!</p>

<p><%= t('podemos.devise.mailer.confirm.p1') %></p>

<p><%= link_to confirmation_url(@resource, confirmation_token: @token), confirmation_url(@resource, confirmation_token: @token) %></p>

<p><%= t('podemos.devise.mailer.confirm.p2') %></p>
